# Remote Control System - Python Dependencies
# 
# Core functionality works with Python standard library only
# These are optional dependencies for enhanced features:

# Screen capture (highly recommended for better performance)
pillow>=9.0.0

# Note: pynput removed due to evdev compilation issues on cloud platforms
# The system will work using Python standard library fallbacks for input control
# Full functionality is available when running the agent on Windows machines
